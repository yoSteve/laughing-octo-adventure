extends ./layout.jade

block content
	a(id='join-waiting', class='but', href='') Join The Wait List!
	ul
		each item in waiting
			if item
				li a(href='/game' data-id=#{item._id}) #{item.username}

